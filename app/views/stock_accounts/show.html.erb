<div class='container'>
  <h3>Stock Account</h3>
  <div class='row col-lg-12 m-0 p-0'>
    <table class="table">
      <tbody>
        <tr>
          <th>Account Number</th>
          <td><%= @stock_account.account_number %></td>
        </tr>
        <tr>
          <th>Stock Name</th>
          <td><%= @stock_account.stock_name %></td>
        </tr>
        <tr>
          <th>Current Balance</th>
          <td><%= @stock_account.balance %></td>
        </tr>
      </tbody>
    </table>
  </div>
  <%= link_to 'Show all', stock_accounts_path, class: 'btn btn-primary'%>
  <%= link_to 'Transaction History', transactions_path(account_id: @stock_account.id), class: 'btn btn-primary' %>
  <%= link_to 'Request Transaction', new_transaction_path(sender_id: @stock_account.id), class: 'btn btn-outline-primary'%>
</div>